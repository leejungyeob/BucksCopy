interface:
  display_name: "BucksCopy L2 Security"
  short_description: "Review credentials, storage, and trust-boundary risks"
  default_prompt: "Use $bucks-copy-l2-security for Bitget credential, Keychain, signature, logging, order, or live-trading security review."
policy:
  allow_implicit_invocation: true
